meta {
  name: Create Customer
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/customers
  body: json
  auth: none
}

body:json {
  {
    "id": null
  }
}

vars:pre-request {
  baseUrl: http://localhost:8080
}
